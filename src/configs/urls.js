const baseUrl = 'https://jsonplaceholder.typicode.com';

const rocketUrl = 'https://api.spacexdata.com/v3';

const urls = {
    users: '/users',
    posts: '/posts',
    comments: '/comments',
    launches: '/launches'
}

export {
    urls,
    baseUrl,
    rocketUrl
}